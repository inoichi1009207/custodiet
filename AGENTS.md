# Example Constitution(示例宪法)

> 本文件是**示例**:收尾闸的每条规则都必须带一个 `law:` 锚点,指回一份人签署过的
> 规范条款——规则引擎在装载期与自测里核「锚点可解析」(INV-2 + law 锚点自测)。
> 换成你自己的项目时,把这三份文件换成你真实的规范,锚点跟着改。
> 设计理由见 `docs/playbook.md` 第 3 步:**没有法源的规则是无主判据,谁都可以删。**

## 恒定条款

每会话默认动作,缺省即执行。本示例保留与闸直接联动的两条:
1. **批级完成条件**:开工 `batch-goal --arm <批号> --cond "<什么算完成>"`,
   收尾逐条写「条件N:达成/未达成/不适用」后 `--clear`(K/K0 两项的法源)。
2. **验证默认**:写批必跑验证套件(G 项④的法条锚点在汇报法「关账前自审一遍」,见 docs/laws/reporting.md;本条是它的宪法层义务句)。

## 执行共识

- 不确定就停下回报;停下等确认必标 `⏸`(S 项认的唯一通用等待标识)。
- 登记临时状态必带失效条件(T 项的法源)。

## 决策边界

需人亲签:不可逆动作、对外发布、守卫语义改动。免签例外(可逆机械改动等)各带成立条件;
**摊给人裁之前先核免签例外**——能自签的别摊,真取舍才上桌(V 项的法源)。

## 承重规则的载体门槛

承重规则不得仅存于按需加载的载体;新立规则须声明其**触发层**
(挂在哪个必然执行的检查点上)——J 项拦「改了规范却没交代触发层」。
