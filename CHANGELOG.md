# Changelog

## 0.4.1 — 2026-09-09

同一天的补丁版。0.4.0 发出后,对 P(承重提交欠三道独立复核)的凭证池做了一轮跨模型 + 两轮红队,
打出四条真缺陷,全部当场修。主线一条:**判据的两半用了不同的量词,于是「有一条就行」变成了「都不用」。**

- **量词不一致(最贵)**:凭证池主路径的开关是「窗口里**任一**动作带时间戳」,而它的豁免是
  「**这一条**没有时间戳」——一条带时间戳的动作就足以让所有不带的条目白拿凭证资格。
  改为窗口动作必须有有效时间戳才入池(fail-closed)。误伤成本已量:两轮红队合计读 93,850 + 1,991 条
  真实转录条目,缺时间戳 0 条,这一支在生产上本就是空集。
- **切点取错**:凭证池只按「本批武装时刻」切,而法条写的是「自上一次承重面提交以来」。
  于是批开头跑一次红队,后面可以无限追加改动并提交而不再要求复跑——**达成它不需要任何伪造,
  只需要不重新武装**;而正常关账会抹掉武装时刻,反倒让判据变严,守规矩的人成本更高。
  现取两者的较大者,比任一单句都严。
- **时间窗只有下界没有上界**:未来时间戳恒在池内。加上界=本轮最晚动作时刻。
- **真值兜底吞掉合法时刻**:`Date.parse` 解析 1970-01-01T00:00:00Z 得 0,而 0 在真值判断里是假,
  于是这个合法时间戳被当成解析失败。两处同族写法一起改成有限性判断。
- **元数据混进证据面**:承诺兑现判据原来整体序列化动作对象,其中的 13 位毫秒时间戳会被当作正文,
  于是「我这就去改 `889`」这类指向短数字的承诺,可能因为某个时间戳恰好含该子串而被判已兑现。
  现在只序列化名称与入参。
- **测试夹具的值没有校验**:新加的时间戳键补了键名校验,值却没有——写 `0` 或空串会被真值判断静默丢弃,
  写非 ISO 串会被解析吞掉,两者都让用例悄悄退回另一条判据路径。两条通道各补有限性校验并抛错。
- 新增 4 条正例钉住上述各格(红队指出它们「写得出却没写」)。

**留档一条方法教训**:验证这批修法时,我自己那支辅助探针把上一轮的条目直接塞进了上下文构造函数的
第一个参数,于是它们全被当成本轮动作、窗口为空——旧代码因此「碰巧」显出预期的三态。真正的用例通道
一直是对的,错的是探针。**自造探针必须与生产夹具走同一条构造通道**,否则读数属于另一个世界。

- 已知未修(登记在案):承重通道的绑定判据只查几个关键词的字面,而那几个词正是提示消息自己教的;
  一个未登记的环境变量可以把另外两条通道的要求整体关掉;批时钟字段可被单独删除而另一条规则察觉不到;
  凭证池的两条路径都还没有变异证人。
## 0.4.0 — 2026-09-09

同步上游 2026-09-05 → 09-09 的闸修复。主线一条:**开工查一次管不了后来——收尾面也要复查并发写。**

- **新规则 CW「收尾时点三查①」**:Stop 时再跑一次判活探针(`scripts/session-triage.mjs --json --fast`,15 s 上限),
  非 PASS(FAIL/UNKNOWN)即提示、不阻断;探针非零退出照样解析 stdout(红灯是业务结果不是执行故障),真超时/无 JSON ⇒ UNKNOWN。
  **本仓不带探针**(它依赖进程枚举与会话转录目录,与宿主强耦合)⇒ 未接探针时本条每轮报一次 UNKNOWN 提示(不阻断),接上自己的即可消除
  (输出 JSON 的 `results[]` 里含 `id:"①"` 一行,`status` 取 PASS/FAIL/UNKNOWN)。
  起因:上游两个写会话并发一整天、互相把对方的半成品扫进自己的提交,全程零闸响(上游台账 D95)。
- **P 项(承重提交欠三道独立复核)重写批边界与凭证池**:凭证按「本批武装时刻」切(动作带转录时间戳),
  回退路径任何提交都切;闸引擎面的独立视角只认红队 agent 且入参须绑定闸文件/规则 id(D11),每批必跑、不随「每 5 次一遍」的宽限。
- **W 用户免审入口 / S 动作面 / I「写前台账任一 false 即新建」**(上游批 182 裁定落地);D99/D100/D101 三条误拦修法。
- 交付物面排除 `clipboard/` 下的点文件(撞额度待续标记每次 `--clear` 被 W 拦一次)。
- README 规则计数改按 `RULES.length` 实数(27 引擎 + 2 内建);此前手写的 23 已漂。
- **撤回过一次的做法(留档)**:上游一度加过「探针不在盘 ⇒ 本条静默退场」三行,以消除未接探针时的提示噪声;红队打回并撤销,理由两条:①改前把探针改名会让本条每轮报 UNKNOWN(**吵**),改后同一动作变成与「无并发写」逐字节不可区分的**静**——等于给一条承重规则装了个无痕开关,且该状态不进任何台账、消音无取证通道;②被援引的「载体不在盘则静默退场」口径,其原文逐字限定「守的是便利性约定,**非承重八族**」,而本条守的是串行写(承重八族)。**结论:提示噪声不值得用一条静默开关去换。**
- 已知未修:CW 的判据本体(判活探针)不随本仓发布;自由 `--check` 与条件对象无结构绑定;探针结果对引擎不可见。
## 0.3.0 — 2026-09-05

同步上游 2026-08-28 → 09-05 的闸修复。主线一条:**「达成」不能再是被审方自己写的字。**

- **`batch-goal` 条件带可执行判据**:`--arm --cond "…" --check "<命令>" --expect "<子串>" | --expect-re "<正则>"`;
  `--clear` 先经 bash 跑判据,不过即拒清并留痕(`clearRefusedAt`),K 项在拒清后不再认「条件N:达成」的自述。
  判据与 arm 台账绑定,同批改判据须 `--force`;`--expect` 短于 4 字符即拒(短匹配走带锚点的正则);正则只跑在输出前 64 KB;
  `--dry-check` 不清账只试跑。起因:一条后台命令先回显上一版的 `ffmpeg exit 0`,再开始渲染新版——grep 日志取到旧行,据此宣布新版已出。
- **新规则 X「落盘断言绑直接探针」**:正文说「X.mp4 已出/落盘/渲染完成」,本轮必须有一条以 X 为直接入参的探针(ffprobe/ls/stat/Read),
  且在最后一次写它之后;grep 日志、产出它的那条 ffmpeg 都不算证据。否定句、引文、占位符、条件句不算断言;媒体文件只认读内容的探针。
- **S 项三处误拦根因**:停工分类词表缺第三类名本身(「实打实阻断」);零动作分支排在「说出下一步」之前使出路②结构性不可达;
  等待类动词(候/等/盯/守/收)不在表内。修后同形态回放不再拦。
- **S 出路②加跨轮兑现核查**:上一窗零动作且含「下一步」承诺、本窗又零动作 ⇒ 出路②失效——「验收中」式空转不能靠每轮说一句下一步无限过闸。
- **新规则 ND「名词日记漏记」**:用户问「X 是什么」而本轮没往 `docs/noun-diary.md` 追加 ⇒ 拦;文件不在盘则静默退场(本仓附示例文件)。
- 其它:`findBash` 扫 PATH;载体面不再按仓户籍剔除(D93 撤销,跨仓外审抓到过真泄漏);留痕写失败大声报而不是静默;
  写面误报形态若干(见上游台账 D91–D98)。
- 已知未修:自由 `--check` 与条件对象无结构绑定;探针结果对引擎不可见(只看入参);journal 与状态文件同一信任域。

## 0.2.0 — 2026-08-27

上游一整天的修复一并同步。主线只有一条,但它反复出现:

**闸承诺的出路,判据不认。**

即:规则被触发时给出「你这样做就能过」,而它的判据其实不接受那个写法。当天在
上游实撞 **13 次**,其中 **3 次是在修这个毛病的过程中新造的**。举几个真实形态:

- 消息说「就地自标『这是推断』」,而词表只认那七个字一字不差——
  写「这条只是我的猜测」照样被拦;
- 消息说「跑个探针」,而判据只认 `node` 一个运行时——Python 仓里跑了探针也算没跑;
- 消息说「派个只读子代理去审」,而法条其实还要 codex/battle 在场才算充分;
- 标记 `⏸ 需要你确认` 是规范写法,但**加粗**成 `⏸ **需要你确认**` 就认不出来了。

### 机制层的答案:出路即数据

规则现在把出路写成 `escapes: [{ say, sample }]`,消息**从它渲染**,
验收台再把每条出路的 `sample` 接到该规则**自己的正例**后面真跑一遍——
说的和做的一旦漂开,套件当场变红。装载期不变量(INV-7)进一步要求:
**凡消息里写了「出路」,就必须声明 `escapes`**,否则拒绝装载。

先例是 doctest:它不漂,是因为**说明里的例子就是被执行的那个东西**。我们做不到
完全同一份,就退一步——给 `say` 里「要照抄的那段字面量」划一个专用标记 `⟪…⟫`,
断言 `sample` 真的含它。(试过直接抠普通引号:27 条里 22 条抠错,因为消息里引号
最多的其实是**反面教材**。标记必须是专为此用的。)

**这套机制的天花板,由一次外审当场指出**(下面「已知缺口」第 1 条):
它证明的是「判据接受这个样例」,**不是**「这个样例跑得起来」。

### 其他

- **判据面归一**:路径比较统一走 `normPath`(反斜杠 / `./` 段 / 重复斜杠);
  **大小写不由它处理**,是下游派生正则带 `i` 标志。载体分类新增「非本仓区」剔除
  (`clipboard/ node_modules/ .git/ dist/ build/ coverage/`)。
- **回声族**:因果族(E0/E1/E2)、S、R 的措辞匹配统一剥引文——引用一句被拦的话
  来处置它,不该被再拦一次。**注意:不是所有规则都剥**,V、O 仍直接匹配原文。
- **新增 O 项(提示级)**:把一个外部页面**当依据用**时,若本轮动作面上既没有
  `WebFetch`、也没有取页核实件的调用 ⇒ 提示。**只认取页,不认搜索**——
  搜索给的是别人对页面的转述。
  **判据的实际强度**:它只看「本轮有没有出现过那类动作」,**不核** URL 是否同一页、
  命令是否真的执行成功。所以它是提示不是证明。它上线第一小时逮到的第一个人是作者本人。
- **消息按环境自适应**:L 与 O 的出路、以及 doc-drift 的执行分支,会先看那个脚本
  在不在,不在就换一句话/跳过,而不是让人去跑一个不存在的命令。
  **仅这三处**——不是全仓所有消息。

### 已知缺口(照实写,不是谦辞)

1. **出路自证不执行任何命令**。它构造一个虚拟 `tool_use` 把命令字符串塞进去,
   看判据认不认。因此**指向不存在文件的出路一样能「自证 PASS」**。
   本版新增 `出路死指针` 普查(只报不判红)作为最便宜的一层补偿:
   在本仓跑它会报出 3 处——`O` 的取页核实件、`E0`/`E2` 的 `verify-laws.mjs`,
   这三个件不随本仓发布。
2. **规则消息里仍有指向上游资产的指针**:`I` 项提到 `docs/tool-register.md`,
   `E0/E2` 的出路提到 `xros:*`,`W` 项提到 `codex-run.mjs` / `battle` / `grill:*`。
   这些在本仓没有安装说明也没有等价降级分支——**照抄本仓 `scripts/` 的人会被指向缺失资产**。
3. **`INV-7` 靠关键词识别「消息里承诺了出路」**(匹配「出路」「就地自标/声明」),
   换个措辞即可绕开。
4. 代码注释里保留了大量上游的欠账编号(`D…`)与批次号。它们对外部读者不可解,
   但**刻意保留**:这个仓的价值有一半在「每条判据为什么长这样」的现场记录,
   抹掉编号会让那些记录失去索引。若你只想要机制、不想要考古,读 `docs/playbook.md`。

## 0.1.1 — 2026-08-26

新增**外部锚** `ledger-anchor`:三链账的前缀指纹定期快照到 agent 仓外,`verify` 比对。

它补的是文件内前向链在结构上防不了的那半,但**限定词要一起读:只覆盖「最后一次快照点之前」**。
`verify` 的判据是「当前文件长度 ≥ 快照时长度,且前 N 字节哈希一致」——所以:

- 锚点**之前**的字节变化(改写 / 插行 / 删行 / 截断)⇒ 红 ——
  **前提有两条**:锚文件本身未被同身份进程改写,且 `verify` 确实被运行过。两条都不是自动成立的。
- 锚点**之后**追加的内容被删掉、而**当前长度仍 ≥ 快照时的长度** ⇒ **verify 返回 OK,检不出**

判据就是「长度 ≥ 快照长度 且 前缀哈希一致」,所以这个洞比「整段砍掉」宽得多:
快照后追加 10 行、只删掉末 3 行,长度仍大于快照值、前缀没动 ⇒ 锚判 OK;
而 `ledger-chain` 对尾部删行剩下的链仍然自洽 ⇒ 也不红。**两件都不覆盖。**

这一句被改了三次,三版都写窄了,依次记在这里:
① 首版写「补上完整尾删」——漏了「只到锚点之前」这个限定词;
② 二版写「当天写、当天尾删」——边界错,正确是**两次快照之间**(写入后若已发生过快照再尾删,
   当前长度小于快照长度,`verify` 会报 TRUNCATED);
③ 三版写「**整段**砍掉、长度**正好**退回快照值」——条件写得比实际窄,读者会推出
   「只删一部分应该逮得到」,而事实恰好相反。现行是第四版。

诚实边界(以头注为准,此处只是摘要,且**不省略会削弱上一句的那条**):
① 同机同用户是**软边界**——锚落在 `~/.custodiet/anchor/`,能以你的身份跑命令的进程也能改它,
   锚自身无认证无链;它提高伪造成本,不是硬隔离。
② 快照间隙内(默认每日)的「写了又擦」检不出。
③ **最新锚点之后的后缀不受任何约束**——即上面那条。
④ 看门狗(锚龄 > 48h 告警)**只在有人跑 `verify` 时才响**;本仓**没有替你接唤醒面**,
   拷走后须自行接一个(否则就是「闸在仓里但不在跑」)。
⑤ `install-task` 目前**只实现了 Windows**(`schtasks`);macOS/Linux 需自己写 cron/launchd。

关于先例:形状参照 Certificate Transparency 的「日志外比对树头」——RFC 6962 §7.3 写明日志可
「presenting two different, conflicting views of the Merkle Tree at different times and/or to
different parties」(内部 Merkle 自洽挡不住),且该违例「is detected by global gossiping」。
出处 <https://www.rfc-editor.org/rfc/rfc6962>,2026-08-26 取页逐字核过。
⚠️ **但反类比要一起读**:CT 那套成立的承重前提是树头由**另一方**持有、跨信任域比对;
本件的锚在同机同用户——先例里唯一让它成立的属性,本件没有。故 CT 只作**形状**参照,
不作强度背书。要那个强度,得把树头收到异机/CI 上。

## 0.1.0 — 2026-08-26

首个公开暂存版。Stop 闸本体(hook-stop-closure,`.claude/settings.json` 单点接线)、
引擎(结构化 ctx / 声明式规则 / 变异验收台)、台账完整性链
(ledger-chain:hash 链 + SIGN-CHECK 呈签尾标)、批级完成条件(batch-goal)、
建闸作业本、示例宪法、中英双 README。

自带的诚实声明:担保边界句以各件头注为准;战报数字为作者自述;
「未观测到同类」是观测陈述,不是不存在证明。
